"use client";
import Link from "next/link";import { usePathname } from "next/navigation";import { useEffect,useState } from "react";import { useWallet } from "./WalletProvider";
const nav=[['/dashboard','◉','Overview'],['/tee-registry','◇','TEE registry'],['/dashboard#score','◌','My score'],['/dashboard#borrow','↗','Borrow']];
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname();const [dark,setDark]=useState(true);const {address,connecting,connect}=useWallet();useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light'},[dark]);const short=address?`${address.slice(0,6)}…${address.slice(-4)}`:'';return <div className="app-shell"><aside className="sidebar"><Link href="/" className="brand"><span className="mark"><i/></span>VELA</Link><Link href="/" className="back">← Back to landing</Link><span className="section-title">WORKSPACE</span><nav>{nav.map(([href,ic,label])=><Link className={`side-link ${path===href?'active':''}`} href={href} key={href}>{ic}&nbsp;&nbsp;{label}</Link>)}</nav><div className="sidebar-bottom"><button className="theme-toggle" onClick={()=>setDark(!dark)} aria-label="Toggle dark mode">{dark?'☼':'◐'} <span>{dark?'Light mode':'Dark mode'}</span></button><button className="wallet-btn" onClick={connect} disabled={connecting}>{connecting?'Connecting…':address?short:'Connect wallet'}</button></div></aside><main className="app-main">{children}</main></div>}
